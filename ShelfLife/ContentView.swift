import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(spacing: 20) {
            Text("ShelfLife")
                .font(.largeTitle)
                .bold()
            Text("Welcome to ShelfLife")
                .foregroundColor(.secondary)
        }
        .padding()
    }
}
